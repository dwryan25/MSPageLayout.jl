
"""For the given page, find the optimal placement
of scholia under the assumptions of the traditional model.
Returns a Vector of `y` coordinates.
$(SIGNATURES)
"""
function model_scholia_layout(pgdata::PageData)
    []
end